export default interface ICardProps {
    // data: unknown;
    isSaved: boolean;
    // isEnabled?: boolean;
    // boxSize: boxSize;
    // chartType?: ChartType;
    // timeInterval?: TimeInterval;
}
